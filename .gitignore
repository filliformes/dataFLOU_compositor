node_modules/
out/
release/
dist-tsc/
.DS_Store
*.log
.env
.env.local

# Personal sessions saved at the repo root during testing
*.dflou.json
/Test*.json

# Portable builds dropped at the repo root after a build
/dataFLOU_compositor.exe
/dataFLOU_compositor.dmg

# Downloaded CI artifacts (gh run download)
/artifacts/

# Claude Code workspace — local scratch, not part of the project.
.claude/

# Local clone of the dataflou library repo for reference reading only.
/124_dataflou-main/

# Theme / typography design exploration. The real app-bundled fonts live
# in src/renderer/src/fonts; these root-level copies + the mockup HTML +
# the font-embedding helper script are design-time artifacts only.
/fonts/
/theme-mockups.html
/embed-fonts.js
